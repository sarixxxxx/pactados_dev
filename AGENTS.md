Use the configured Stitch MCP server whenever a task references Google Stitch screens, assets, or generated UI code for this project.

When improving the landing page, prefer pulling the Stitch screen output first and then adapting the implementation to the existing Next.js app instead of recreating the design blindly.
